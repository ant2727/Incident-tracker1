spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/incident_tracker}
    username: ${DB_USERNAME:incident_tracker}
    password: ${DB_PASSWORD:incident_tracker}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
